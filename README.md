# gridfleet-site

Public landing page for [GridFleet](https://gridfleet.ai) â€” the multi-agent execution layer.

Static HTML, no build step. Served via GitHub Pages.

The platform itself lives in a separate (private) repository.
